# 项目进度（PROGRESS）

> **这是项目的单一事实源（Single Source of Truth）。**
> 任何会话/任何接手人，**第一步**读本文件；需要细节再按索引读对应文档。
> 每个阶段结束或状态变化时，**必须更新本文件**并提交。
> 最后更新：2026-09-20

---

## 1. 项目一句话

上海市政府采购网（政采云）信息采集与销售提醒系统：每日 17:00 采集三类公告（意向公开/招标/中标）入库，按行政区域匹配销售，每日邮件提醒；数据带多维标签支撑分析。部署于信创服务器（x86_64 Ubuntu + PostgreSQL 16）。

## 2. 当前状态（一眼看）

| 项 | 状态 |
|---|---|
| 当前阶段 | **M5（清单自维护）** — ✅ 用户验收通过（2026-09-20） |
| 阻塞项 | 下一步 M6 部署上线；M2 全量日采集未跑（已知问题）；应用进程尚未部署到信创机 |
| 最近验证 | `{test_cmd}` = `npm test` → **41/41**；用户确认管理界面测试正常 |

## 3. 阶段总览

| 阶段 | 内容 | 状态 | 完成判定（DoD） |
|---|---|---|---|
| M1 | 数据源打通 + 采集原型 | ✅ 完成 | 列表/详情/正文解析跑通；抽样 15 条字段校准 ≥99%；全量日采集验证 |
| M2 | 建库入库 | ✅ 抽检关闭 | 7 表+视图；幂等 upsert；detail_status。用户 2026-09-18 要求进入下一步。**已知问题：全量约 224 条日采集未跑** |
| M3 | 标签与分析底座 | ✅ 完成 | 自动标签落库；`v_daily_summary` + `v_by_district_type`；`src/query.js` JSON 查询。用户 2026-09-18 手测确认 |
| M4 | 日报与邮件 | ✅ 完成 | 区域匹配；日报生成；SMTP；防重发。用户 2026-09-18 确认全市日报收件无误 |
| M5 | 清单自维护 | ✅ 完成 | 登录+销售清单增删改/CSV+公告人工标签。用户 2026-09-20 确认测试正常。仪表盘/日报/采集/设置未做（ADR-015） |
| M6 | 部署上线 | ⬜ 未开始 | systemd 定时；监控告警；备份；试运行 1 周 |

## 4. M1 完成记录（证据）

- 代码：`src/`（适配器/解析/归一化/标签/CLI），运行 `node src/index.js --date YYYY-MM-DD`
- 全量验证：2026-08-14 → 意向 68 + 招标 68 + 中标 88 = **224 条/日**，日期纯净 100%
- 字段校准：区划/采购人 15/15；意向明细 8/8；招标预算/开标/资金 5/5；中标金额+供应商 ✅
- 详细结论：`docs/M1采集原型-校准结论.md`

## 5. M5 任务清单

- [x] 登录（`.env` 单账号 + session）
- [x] 销售清单增删改、停用
- [x] CSV 导入（非法行跳过并报告）/ 导出 / 模板
- [x] 公告查询 + 详情 + 人工标签增删
- [x] `{test_cmd}` 全绿（41/41）
- [x] **用户验收**：2026-09-20 用户确认管理界面测试正常

### 下一步：M6 部署上线（未开始）

systemd 定时采集/日报；管理界面常驻；监控告警；备份；试运行 1 周。

M4 已关闭。仪表盘 / 日报记录 / 采集任务 / 系统设置不做（ADR-015）。

成功标准（可证伪）：
1. 未登录访问 `/sales` 或 `/api/sales` → 跳转登录或 HTTP 401
2. 登录后新增一行合法销售（姓名+邮箱+标准区名）→ 列表出现；删除该行 → 列表消失
3. 导入 CSV（1 条合法 + 1 条非法区名）→ 报告成功 1 / 失败 1
4. 导出 CSV 表头含 `sales_name,sales_email,region`
5. 公告详情添加 `product_line` 标签 → 刷新后仍在；删除 → 消失
6. `npm test` 全绿

```
node src/admin.js
# 浏览器打开 http://127.0.0.1:8080  账号见 .env ADMIN_USER / ADMIN_PASS
```

## 6. 运行与验证

```
npm test
node src/db/migrate.js
node src/index.js --date YYYY-MM-DD --limit 2
node src/query.js search --district 长宁区
node src/query.js summary --from 2026-09-17 --to 2026-09-17
node src/digest.js run --date 2026-09-17
node src/admin.js
```

DATABASE_URL 只放 `.env`（gitignore）。库在 `10.12.64.101:5432`，库名/用户 `zfcg`。

## 7. 待用户提供（阻塞 M6 定时部署）

| 项 | 用途 | 状态 |
|---|---|---|
| 区域-销售清单 CSV（姓名+邮箱+区域） | 日报区域匹配 | **已导入** 赵海东 / 嘉定区 |
| 公司 SMTP 配置（主机/端口/账号/发件人） | 邮件发送 | **已填 .env**；全市日报实发 sent |
| 信创服务器 | 部署采集定时任务 | **已有** `10.12.64.101`（Ubuntu 24.04 + PostgreSQL 16.15）；应用进程尚未部署到该机 |

## 8. 已确认决策（详见 `docs/决策日志.md`）

- 范围仅上海；招标/磋商纳入（ZcyAnnouncement2）
- 数据库 PostgreSQL 16；信创 x86_64 + Ubuntu；**现网库 10.12.64.101**
- 意向明细拆表；标签双通道；日期客户端强制过滤
- jsonb 入库统一 `JSON.stringify` + `::jsonb`（ADR-011）
- M3 查询走 CLI JSON，HTTP 管理界面放到 M5（ADR-012）
- M4：空区域不发；管理员收全市（ADR-014）；SMTP 465 SSL
- M5：一期只做登录+销售清单+公告打标签；`.env` 单账号；SSR HTML（ADR-015）

## 9. 文件索引（读文件顺序）

1. **本文件**（PROGRESS.md）— 状态与下一步
2. `docs/会话恢复协议.md` — 新会话接续方法（含验证命令）
3. `docs/决策日志.md` — 已定决策，勿重谈
4. 按需：`docs/数据库设计说明书.md` / `docs/M1采集原型-校准结论.md` / `docs/政府采购信息采集与销售提醒系统-设计方案.md` / `docs/管理界面设计.md`（M5 控制端页面与交互）
